#!/bin/sh
set -e
[ -z "$1" ] && { >&2 echo "usage: ${0##*/} <new_script>"; exit 1; }

[ ! -e "$1_test" ] && {
	sed "s#PROGRAM#$1#g" "$HOME/bin/_test" > "$1_test"
	chmod +x "$1_test"
}

[ ! -e "$1" ] && {
	echo '#!/bin/sh' > "$1"
	chmod +x "$1"
}
